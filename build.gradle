apply plugin: 'groovy'
apply plugin: 'maven'

group = 'ru.shadam.revolver'
version = '0.1-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
	compile gradleApi()
	compile localGroovy()
    testCompile 'junit:junit:4.11'
}

task wrapper(type: Wrapper) {
	gradleVersion = '2.0'
}